apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.6
version = '0.1'
group = 'jsr223'

defaultTasks 'jar'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}